#!/usr/bin/env bash
#MISE description="Open a bash shell in the pi container with the same mounts as the pi task"
#MISE raw=true
#MISE dir="{{cwd}}"
set -euo pipefail

# shellcheck source=tasks/pi/_common
source "$(dirname "$0")/_common"

docker run --entrypoint bash "${DOCKER_FLAGS[@]}" "${PI_IMAGE}"
